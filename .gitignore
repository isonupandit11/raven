# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
.npm-cache

# Build output
dist
dist-ssr
dist-electron
out
.build
*.tsbuildinfo

# Database files
*.db
*.db-journal
*.sqlite
*.sqlite3

# Test coverage
coverage

# Playwright
test-results/
playwright-report/

# Environment and secrets
.env
.env.*
.env.local
.env.*.local
*.pem

# Native build artifacts (Swift)
.swiftpm/
*.swiftmodule
*.swiftdoc

# Native AEC addon build artifacts
src/native/aec/build/
src/native/aec/deps/
src/native/aec/node_modules/

# Native Windows audio module build artifacts (Rust / NAPI-RS).
# `napi build` emits the .node plus an index.js/index.d.ts binding shim, and
# cargo's target/ runs to hundreds of MB - none of it belongs in the repo.
# Without these, a first Windows build leaves six untracked entries including
# a ~500 KB binary.
src/native/windows/target/
src/native/windows/node_modules/
src/native/windows/*.node
src/native/windows/index.js
src/native/windows/index.d.ts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS files
Thumbs.db

# Build artifacts
release/

# Premium features (only tracked on the premium branch)
src/pro/
backend/
infra/
docs/*.md
.github/SECRETS.md
.github/workflows/deploy-backend.yml
.github/workflows/release-electron.yml

# Project-specific
RavenPlan.txt
cluely/

# Old placeholder tray icons (quarantine-locked, replaced by icon*.png)
resources/tray/trayTemplate.png
resources/tray/trayTemplate@2x.png
resources/tray/trayActiveTemplate.png
resources/tray/trayActiveTemplate@2x.png
